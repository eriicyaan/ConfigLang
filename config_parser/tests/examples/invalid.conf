<#
Invalid configuration for error testing
#>

// Undefined constant
BROKEN := .[UNKNOWN 2 +].;

// Division by zero
INVALID := .[1 0 /].;

// Invalid expression
MALFORMED := .[1 2 3 +].;

struct {
    // Invalid reference
    bad_value = UNKNOWN_CONSTANT,

    // Invalid expression in struct
    calculation = .[10 0 /].,
}