<#
Basic configuration example
#>

MAX_CONNECTIONS := 100;
TIMEOUT := 30;

struct {
    Server = struct {
        Host = "localhost",
        Port = 8080,
        Max_connections = MAX_CONNECTIONS,
        Settings = struct {
            Timeout = TIMEOUT
        }
    }
}